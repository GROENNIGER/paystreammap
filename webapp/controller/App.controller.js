sap.ui.define([
  "sap/ui/core/mvc/Controller"
], (BaseController) => {
  "use strict";

  return BaseController.extend("rtl.paystreammap.paystreammap.controller.App", {
      onInit() {
      }
  });
});